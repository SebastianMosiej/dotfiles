bind main B >!git rebase -i %(commit)
bind main F >!git commit --fixup=%(commit)
bind main P >!git reset -p HEAD~
bind status P >!?git push --force-with-lease

bind generic + >!git commit --amend
bind refs C >!git checkout %(branch)

