
#check if root passwd is set
PASSWD_STATUS=$(sudo passwd -S root 2>/dev/null)
if [ $? -eq 0 ]; then
  status_field=$(echo "$passwd_status" | awk '{print $2}')
  if [ "$status_field" != "P" ]; then
    echo "Root password is not set - please set it now" 
    sudo passwd root
  fi
fi



